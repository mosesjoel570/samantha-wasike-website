import {
  Stethoscope,
  HeartPulse,
  Baby,
  Brain,
  Ambulance,
  Microscope,
} from "lucide-react";

const services = [
  {
    icon: <Stethoscope size={45} />,
    title: "General Consultation",
  },
  {
    icon: <HeartPulse size={45} />,
    title: "Cardiology",
  },
  {
    icon: <Baby size={45} />,
    title: "Pediatrics",
  },
  {
    icon: <Brain size={45} />,
    title: "Neurology",
  },
  {
    icon: <Ambulance size={45} />,
    title: "Emergency Care",
  },
  {
    icon: <Microscope size={45} />,
    title: "Laboratory",
  },
];

export default function Services() {
  return (
    <section id="services" className="bg-white py-24">
      <div className="mx-auto max-w-7xl px-6">

        <h5 className="text-center font-semibold uppercase tracking-widest text-blue-700">
          Our Services
        </h5>

        <h2 className="mt-4 text-center text-5xl font-bold">
          Comprehensive Medical Care
        </h2>

        <div className="mt-16 grid gap-8 md:grid-cols-3">
          {services.map((service) => (
            <div
              key={service.title}
              className="rounded-2xl border bg-white p-10 text-center shadow-sm transition hover:-translate-y-2 hover:bg-blue-700 hover:text-white hover:shadow-xl"
            >
              <div className="mb-6 flex justify-center">
                {service.icon}
              </div>

              <h3 className="text-2xl font-semibold">
                {service.title}
              </h3>
            </div>
          ))}
        </div>

      </div>
    </section>
  );
}