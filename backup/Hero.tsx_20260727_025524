import { motion } from "framer-motion";
import { CheckCircle2 } from "lucide-react";

export default function Hero() {
  const features = [
    "Experienced Healthcare Team",
    "Modern Medical Equipment",
    "Quality Patient Care",
    "Affordable Services",
    "Friendly Environment",
  ];

  return (
    <section
      id="home"
      className="relative overflow-hidden bg-gradient-to-r from-blue-900 via-blue-800 to-cyan-700 text-white"
    >
      <div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-16 px-6 py-28 lg:flex-row">

        {/* Left Side */}
        <motion.div
          initial={{ opacity: 0, x: -50 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.8 }}
          className="max-w-2xl"
        >
          <p className="font-semibold uppercase tracking-[5px] text-cyan-200">
            Compassion | Excellence | Care
          </p>

          <h1 className="mt-6 text-5xl font-extrabold leading-tight lg:text-6xl">
            Welcome to
            <br />
            Dr. Samantha Hospital
          </h1>

          <p className="mt-8 text-xl leading-8 text-blue-100">
            Professional healthcare delivered with compassion,
            modern technology, and experienced medical professionals
            dedicated to your well-being.
          </p>

          <div className="mt-10 flex flex-wrap gap-5">
            <a
              href="#contact"
              className="rounded-lg bg-white px-8 py-4 font-semibold text-blue-900 transition hover:bg-blue-100"
            >
              Book Appointment
            </a>

            <a
              href="#services"
              className="rounded-lg border border-white px-8 py-4 transition hover:bg-white hover:text-blue-900"
            >
              Our Services
            </a>
          </div>
        </motion.div>

        {/* Right Side */}
        <motion.div
          initial={{ opacity: 0, x: 60 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 1 }}
          className="w-full max-w-md"
        >
          <div className="rounded-3xl bg-white p-10 text-gray-800 shadow-2xl">

            <h2 className="text-3xl font-bold text-blue-700">
              Why Choose Us?
            </h2>

            <p className="mt-3 text-gray-600">
              Trusted healthcare with experienced professionals,
              advanced technology, and compassionate service.
            </p>

            <div className="mt-8 space-y-5">
              {features.map((item) => (
                <div
                  key={item}
                  className="flex items-center gap-4 rounded-xl border border-gray-100 p-3 transition hover:bg-blue-50"
                >
                  <CheckCircle2 className="h-6 w-6 text-green-500 flex-shrink-0" />

                  <span className="font-medium text-gray-700">
                    {item}
                  </span>
                </div>
              ))}
            </div>

          </div>
        </motion.div>

      </div>
    </section>
  );
}